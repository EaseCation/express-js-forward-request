# express-js-forward-request
A simple Express.js script used to forward all web request to another API. Useful for hosting service in dynamic IP address but API provider require static IP whitelist, e.g. WeChat Official accounts.
